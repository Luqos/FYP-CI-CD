# AWS Credentials & Certificates
secrets.h
certs/
*.pem
*.key
*.crt

# Environment variables & Local Frontend Config
.env
frontend/config.js

# Terraform artifacts & state
.terraform/
*.tfstate
*.tfstate.*
# NOTE: .terraform.lock.hcl is intentionally NOT ignored.
# It MUST be committed to the repo so CI/CD uses identical provider versions.

# Python virtual environment & cache
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/

# Build artifacts (regenerated by CI/CD)
build/
*.zip

# OS & Editor files
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/